[tools]
bun = "latest"
go = "latest"
hyperfine = "latest"

[tasks.bench]
run = [
    "cd bun && bun install && bun run build",
    "cd go && go build",
    "cd .. && zig build benchmark",
    "bun run run.ts",
]
description = "Build all benchmarks and compare them with hyperfine"
