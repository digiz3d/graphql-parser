"""
Nice object
"""
type FirstObject {
  name: String!
  material: String!
}

type SecondObject {
  "this is a new field"
  firstProperty: String!
  secondProperty: String!
}

union FirstUnion = Yup | Nope

union SecondUnion = Yup2 | Nope2

interface FirstInterface {
  field1: String!
  "with some description"
  field2(input: ID!): String!
}

interface SecondInterface {
  field1: ID!
  field2: ID!
}

input FirstInput {
  field1: ID!
  field2: ID!
}

input SecondInput {
  field1: ID!
  field2: ID!
}
