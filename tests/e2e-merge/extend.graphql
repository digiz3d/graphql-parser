#ok
extend type FirstObject {
  material: String!
}

type SecondObject {
  secondProperty: String!
}

union FirstUnion = Nope

extend union SecondUnion = Nope2

interface FirstInterface {
  "with some description"
  field2(input: ID!): String!
}

extend interface SecondInterface {
  field2: ID!
}

input FirstInput {
  field2: ID!
}

extend input SecondInput {
  field2: ID!
}